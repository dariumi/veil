{
  "permissions": {
    "allow": [
      "Bash(cargo build:*)",
      "Read(//usr/local/bin/**)",
      "Bash(~/.cargo/bin/cargo build:*)",
      "Bash(pkg-config --libs openssl)",
      "Read(//usr/include/openssl/**)",
      "Bash(OPENSSL_DIR=/usr ~/.cargo/bin/cargo check -p veil-core)",
      "Bash(git commit -m 'fix: resolve compile errors and clean up all warnings:*)"
    ]
  }
}
